% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_unit.R
\name{get_unit}
\alias{get_unit}
\title{Get unit information}
\usage{
get_unit(
  name = NULL,
  code = NULL,
  state = NULL,
  region = NULL,
  type = NULL,
  links = FALSE
)
}
\arguments{
\item{name}{Optional character vector of full or partial unit names (e.g.
\code{"kenai"}, \code{"kenai nwr"}).}

\item{code}{Optional character vector of unit codes (e.g. \code{"FF07RYKD00"}).
Case-insensitive.}

\item{state}{Optional two-letter state code(s) to filter by (e.g. \code{"AK"}).
A unit matches if any of its state codes matches any supplied value.
Case-insensitive; vectors match any.}

\item{region}{Optional FWS region(s) to filter by. Accepts the region number
(e.g. \code{7}) or the full region code (e.g. \code{"R0007"}); vectors match any.}

\item{type}{Optional unit type(s) to filter by, matched against the
\code{type_name} field (e.g. \code{"refuge"}). Case-insensitive substring match;
vectors match any.}

\item{links}{Logical. If \code{TRUE}, include the direct and indirect link
list-columns (\code{direct_links}, \code{direct_inactives}, \code{indirect_links},
\code{indirect_inactives}). Defaults to \code{FALSE}.}
}
\value{
A tibble of unit fields. When \code{links = TRUE}, four linkage
list-columns are appended.
}
\description{
Returns unit records as a tibble. With no arguments, returns all units.
Arguments filter the set: \code{name} matches unit names, \code{code} matches unit
codes, \code{state} matches the unit's state code(s), \code{region} matches the FWS
region, and \code{type} matches the unit type (e.g. "refuge"). Units matching any
supplied \code{name} or \code{code} are kept, then narrowed to those also matching
\code{state}, \code{region}, and \code{type}. Name and type matching are case-insensitive
and tolerant of partial values; name matching also treats "national wildlife
refuge", "nwr", and "refuge" as equivalent.
}
\details{
The API's \verb{/api/Unit/\{code\}} endpoint is documented as experimental and
returns a placeholder for every input, so it is not used. Units are looked up
in the authoritative \verb{/api/Unit/unitselector} listing, which is cached for
the session (clear it with \code{\link[=refresh_units]{refresh_units()}}).
}
\examples{
\dontrun{
get_unit()                              # all units
get_unit(name = "kenai")
get_unit(code = "FF07RYKD00")
get_unit(name = "kenai nwr", code = "FF07RYKD00")
get_unit(state = "AK")
get_unit(region = 7)
get_unit(type = "refuge")               # refuges only
get_unit(region = 7, type = "refuge")
get_unit(code = "FF07RYKD00", links = TRUE)
}
}

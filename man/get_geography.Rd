% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_geography.R
\name{get_geography}
\alias{get_geography}
\title{Get geography for units by name, code, state, region, or type}
\usage{
get_geography(
  name = NULL,
  code = NULL,
  state = NULL,
  region = NULL,
  type = NULL,
  crs = 4326,
  geometry = TRUE
)
}
\arguments{
\item{name}{Optional character vector of full or partial unit names (e.g.
\code{"kenai"}, \code{"kenai nwr"}).}

\item{code}{Optional character vector of unit codes (e.g. \code{"FF07RYKD00"}).
Case-insensitive.}

\item{state}{Optional two-letter state code(s) to filter by (e.g. \code{"AK"}).
A unit matches if any of its state codes matches any supplied value.
Case-insensitive; vectors match any.}

\item{region}{Optional FWS region(s) to filter by. Accepts the region number
(e.g. \code{7}) or the full region code (e.g. \code{"R0007"}); vectors match any.}

\item{type}{Optional unit type(s) to filter by, matched against the
\code{type_name} field (e.g. \code{"refuge"}). Case-insensitive substring match;
vectors match any.}

\item{crs}{Coordinate reference system for the returned geometries, passed to
\code{\link[sf:st_sf]{sf::st_sf()}}. Defaults to \code{4326} (WGS84), the CRS of the API's WKT output.}

\item{geometry}{Logical. If \code{TRUE} (default), return an \pkg{sf} object. If
\code{FALSE}, return a plain tibble with the raw WKT \code{geography} column.}
}
\value{
An \pkg{sf} object (or tibble if \code{geometry = FALSE}) with columns
\code{code}, \code{full_name}, and geometry (or a WKT \code{geography} column). Units with
no geography yield empty geometries.
}
\description{
Returns unit geography from the FWS Unit REST API as an \pkg{sf} object. With
no arguments, returns geography for all units. Otherwise keeps units matching
any supplied \code{name} or \code{code}, then narrows to those also matching \code{state},
\code{region}, and \code{type}. Name and type matching are case-insensitive and
tolerant of partial values; name matching also treats "national wildlife
refuge", "nwr", and "refuge" as equivalent.
}
\examples{
\dontrun{
get_geography()                        # all units
get_geography(name = "kenai")
get_geography(code = "FF07RYKD00")
get_geography(state = "AK")
get_geography(region = 7)
get_geography(type = "refuge")         # refuges only
get_geography(code = "FF07RYKD00", geometry = FALSE) # raw WKT tibble
}
}
